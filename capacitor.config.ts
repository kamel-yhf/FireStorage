import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.firestorage',
  appName: 'firestorage',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
